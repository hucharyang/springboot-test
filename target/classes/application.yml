spring:
  application:
    name: springboot-test
  profiles:
    active: dev
---
server:
  servlet:
    context-path: /wanho
  port: 8899
spring:
  config:
    activate:
      on-profile: test
---
---
# ��������
server:
  port: 8083
spring:
  config:
    activate:
      on-profile: dev
